
.error,
.notice, 
.success    { padding: .8em; margin-bottom: 1em; border: 2px solid #ddd; }

.error      { background: #FBE3E4; color: #8a1f11; border-color: #FBC2C4; }
.notice     { background: #FFF6BF; color: #514721; border-color: #FFD324; }
.success    { background: #E6EFC2; color: #264409; border-color: #C6D880; }
.error a    { color: #8a1f11; }
.notice a   { color: #514721; }
.success a  { color: #264409; }

/* typo settings */
body {
	font-family: 'Ubuntu', sans-serif;
	background: #EEE;
}
h1,h2,h3,h4,h5,h6,strong {
}
em {
}


img {
	max-width: 100%;
}








#google-map {
	height: 300px;
}

img {
	max-width: 100%;
}







  .clear {
    clear: both;
    display: block;
    overflow: hidden;
    visibility: hidden;
    width: 0;
    height: 0;
  }


.rond ul {
  margin: 0;
  padding: 0;
}
.rond li {
  margin: 0;
  padding: 0;
  list-style: none;
  width: 200px;
  height: 400px;
  border: 1px dashed red;
  position: absolute;
  top: 50%;
  left: 50%;
  transform-origin: 100% 100% 0;
}
.rond li.rond-ecologiste { transform: rotate(-10deg) skew(50deg); }
.rond li.rond-libertaire { transform: rotate(30deg) skew(50deg); }
.rond li.rond-humaniste { transform: rotate(70deg) skew(50deg); }
.rond li.rond-liberal { transform: rotate(110deg) skew(50deg); }
.rond li.rond-socialiste { transform: rotate(150deg) skew(50deg); }
.rond .card {
  width: 200px;
  height: 200px;
  background: #ccc;
  border-radius: 60%;
  padding: 1em;
  transform: skew(-50deg) rotate(-70deg) scale(1);
  text-align: center;
}
.rond li.rond-ecologiste .card {background: #7DA702; }
.rond li.rond-libertaire .card {background: #9A1B7E; }
.rond li.rond-humaniste .card {background: #FB6B02; }
.rond li.rond-liberal .card {background: #08589D; }
.rond li.rond-socialiste .card {background: #E20040; }
.rond h1 {
  
}
.rond p {
  display: none;
}



.footer {
	background: #C8D08A;
	border-bottom: 5px solid #1E5A41;
	padding: 1em 0;
	color: #1E5A41;
	position: absolute;
	bottom: 0;
	left: 0; 
	width: 100%;
}
.footer a {
  color: #1E5A41;
	
}

.footer-left, .footer-right {
	float: left;
	width: 50%;
	padding: 0 1em;
	-webkit-box-sizing: border-box;
  -moz-box-sizing: border-box;
  box-sizing: border-box;
}
.footer-right {
  text-align: right;
  font-size: .8em;
}